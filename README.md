# Powershell Cmdlet for Hazelcast .Net Client 

This CMDLET can be used to make simple load tests 

```
    
PS > .\benchmark.ps1 -clientVersion "Hazelcast Client DLL folder to test" -address "initial server address"

```
